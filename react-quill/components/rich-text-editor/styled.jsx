import styled from 'styled-components'
import { Typography } from 'antd'

const { Text } = Typography
export const StyledText = styled(Text)`
  .label-form-input-required {
    color: red;
    margin-left: 0.2rem;
  }
`
export const StyledRishText = styled.div`
  .ql-container {
    min-height: 10rem;
  }

  .ql-editor {
    min-height: 10rem;
  }
`
